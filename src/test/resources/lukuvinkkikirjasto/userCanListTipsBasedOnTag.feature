Feature: User can view tips as a list filtered by a tag

    Scenario: There is a option in the main menu to list tips based on a tag

    Scenario: Only tips with selected tag are shown 

    Scenario: User types desired tag to UI